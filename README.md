This application is an implementation of fetching APIs using two different ways ('fetch' and 'axios').

I have given an option to choose number of users to fetch and to choose which way to fetch data.